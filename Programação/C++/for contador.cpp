#include <stdio.h>;
#include <windows.h>;

int main(){
	
	int contador;
	for (contador = 0; contador <10; contador ++);
	{ 
	printf("Contador: %d\n",contador);
	}
	printf("Valor do Contador apos o laco: %d\n",contador);
}
